
#include "main.h"
#include <stdio.h>

/**
 * main - check the code.
 *
 * Return: Always 0.
 */
int _isupper(int c);
{
    char c;
    if (c == "A")
    {
	    printf(1)
    }
    else
	    printf(0)
    return (0);
}
